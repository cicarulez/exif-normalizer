{
  "name": "exif-normalizer",
  "version": "0.1.0",
  "description": "CLI for normalizing photo EXIF metadata safely.",
  "type": "module",
  "bin": {
    "exif-normalizer": "./src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "exiftool-vendored": "^29.3.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
